/* =====================================================================
   AIパーク : 共有機能（Firebase）の設定ファイル
   ---------------------------------------------------------------------
   ここに Firebase の設定を貼り付けると「🌐 みんなで共有モード」になります。
   空のままだと「💾 このPCのみモード」で動きます（設定しなくても使えます）。

   ▼ 取得手順は SETUP.md（かんたん設定ガイド）を見てください。
      Firebase コンソールでコピーできる firebaseConfig の中身を、
      下の { } の中にそのまま貼り付けるだけです。
   ===================================================================== */

window.AIPARK_FIREBASE_CONFIG = {
  // apiKey: "AIza..................",
  // authDomain: "your-project.firebaseapp.com",
  // projectId: "your-project",
  // storageBucket: "your-project.appspot.com",
  // messagingSenderId: "000000000000",
  // appId: "1:000000000000:web:xxxxxxxxxxxx"
};
