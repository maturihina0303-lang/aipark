# AIパーク かんたん設定ガイド（みんなで共有する）

このサイトは **2つのモード** で動きます。

| モード | 状態 | 説明 |
|--------|------|------|
| 💾 このPCのみ | 設定なし（初期状態） | 投稿はこのブラウザだけに保存。すぐ使えます |
| 🌐 みんなで共有 | Firebase を設定 | 投稿を全員で共有。**どのPCからでも同じ一覧が見えます** |

「みんなで共有」にするには、下の 3 ステップだけ行ってください。無料でできます。

---

## ステップ 1｜Firebase プロジェクトを作る（5分）

1. https://console.firebase.google.com/ を開き、Googleアカウントでログイン
2. 「**プロジェクトを追加**」→ 名前を入力（例: `aipark`）→ 続行
   - 「Google アナリティクス」は **オフ**でOK
3. 作成が終わったら、そのプロジェクトを開く

## ステップ 2｜データベース（Firestore）を用意する

1. 左メニュー **構築 → Firestore Database** を開く
2. 「**データベースの作成**」をクリック
3. ロケーションは `asia-northeast1（東京）` などを選択 → 次へ
4. 「**テストモードで開始**」を選んで作成（あとでルールは下記に差し替え）
5. 上部タブ **「ルール」** を開き、中身を次に置き換えて「**公開**」：

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
```

> `sites`（投稿）と `settings`（合言葉）の両方を許可する設定です。

> このルールは「誰でも読み書きOK」です。仲間内の小さなサイト向けの設定です。
> 不特定多数に URL を配る場合は、荒らし対策が別途必要になります（下の「補足」参照）。

## ステップ 3｜設定を `firebase-config.js` に貼る

1. Firebase の **⚙️（プロジェクトの設定）→ 全般** を開く
2. 下の方「マイアプリ」で **`</>`（ウェブ）** アイコンをクリック
3. アプリ名を入力（例: `aipark-web`）→ 登録（Hosting はチェック不要）
4. 表示される `const firebaseConfig = { ... }` の **{ } の中身**をコピー
5. このフォルダの **`firebase-config.js`** を開き、次のように貼り付ける：

```js
window.AIPARK_FIREBASE_CONFIG = {
  apiKey: "AIza...ここに実際の値...",
  authDomain: "aipark-xxxx.firebaseapp.com",
  projectId: "aipark-xxxx",
  storageBucket: "aipark-xxxx.appspot.com",
  messagingSenderId: "1234567890",
  appId: "1:1234567890:web:abcdef123456"
};
```

保存してサイトを開き直すと、左上のバッジが **🌐 みんなで共有中** になれば成功です。

---

## ほかの人にも見せる（サイトを公開する）

共有データ（Firebase）だけでなく、**サイト本体を置く場所**も必要です。おすすめは無料の以下いずれか：

- **Netlify Drop** … https://app.netlify.com/drop にこの `作業場` フォルダをドラッグ＆ドロップするだけ。すぐ URL が発行されます（一番かんたん）
- **Firebase Hosting** … 同じ Firebase 上で公開できます（`firebase deploy`）
- **GitHub Pages** … GitHub にアップして公開

発行された URL を仲間に配れば、**どのPC・スマホからでも同じ一覧**が見られます。

---

## 合言葉ロック（知っている人だけ入れる）

サイトを開くと **合言葉の入力画面** が出ます。

- **初回**：オーナーが合言葉を決めます（画面で入力するだけ。設定ファイルの編集は不要）
  - 共有モードなら、この合言葉は全員共通になります
- **2回目以降**：みんなは決められた合言葉を入力すると入れます
- 「**この端末を記憶する**」にチェックすると、次回から入力を省略できます
- ヘッダーの「**🔒 ロック**」でいつでも再ロック、「**合言葉変更**」で変更できます
- ロックしたくない場合は初回画面の「スキップ」でロックなしにもできます

合言葉はそのまま保存されず、**ハッシュ化**して保存されます（元の文字は復元できません）。

> **強さについて**：これは「知らない人が UI から入るのを防ぐ」ソフトなロックです。
> 上の Firestore ルールは「誰でも読み書きOK」なので、技術に詳しい人がデータベースへ
> 直接アクセスすれば中身は見られます。**仲間内で使う分には十分**ですが、
> 不特定多数へ URL を配って厳密に守りたい場合は、ログイン制（Firebase Authentication）に
> する必要があります。必要になったら相談してください。

## 補足・注意

- **合言葉について**：合言葉は共有データベースに保存され、このサイトを見られる人（＝メンバー）には表示できます。画面上は伏せ字ですが、「メンバー全員に共有される前提のメモ」として扱ってください。**銀行やSNSの本当のパスワードは入れないでください。**
- **画像**：サムネイル画像は自動で圧縮して保存します。とても大きい画像は「大きすぎます」と出ることがあります。その場合は小さめの画像を選んでください。
- **セキュリティを上げたい場合**：不特定多数に公開するなら、Firebase Authentication（ログイン制）を足す、書き込みに簡易パスワードを付ける、などの拡張ができます。必要になったら相談してください。
